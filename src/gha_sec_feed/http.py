"""HTTP client with egress allowlist, identity headers, and retry policy."""


def get(url: str) -> bytes:
    """Fetch a URL through the allowlisted HTTP client. Stub — implemented in phase 2a."""
    raise NotImplementedError(url)
